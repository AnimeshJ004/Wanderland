<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
  <div class="container-fluid px-4">
    <a class="navbar-brand d-flex align-items-center" href="/home">
      <i class="fa-solid fa-compass fa-2x me-2" style="color: #fe424d;"></i>
      <span class="fw-bold fs-4" style="color: #222222;">WanderLust</span>
    </a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav me-auto">
        <a class="nav-link fw-semibold px-3 py-2 mx-1 rounded-pill" href="/listings" style="color: #222222; transition: all 0.3s ease;">
          <i class="fa-solid fa-magnifying-glass me-1"></i>Explore
        </a>
        <a class="nav-link fw-semibold px-3 py-2 mx-1 rounded-pill" href="/listings/new" style="color: #222222; transition: all 0.3s ease;">
          <i class="fa-solid fa-plus me-1"></i>Add Listing
        </a>
      </div>
      <div class="navbar-nav ms-auto d-flex align-items-center">
        <div class="search-container me-3">
          <input class="form-control search-input border-0 rounded-pill ps-3" type="search" placeholder="Search destinations..." aria-label="Search" style="border-color: #dee2e6; background-color: #f8f9fa; display: none;">
          <button class="btn search-toggle border-0 rounded-pill px-3" type="button" style="color: #fe424d;">
            <i class="fa-solid fa-search"></i>
          </button>
        </div>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle fw-semibold px-3 py-2 rounded-pill" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="color: #222222; transition: all 0.3s ease;">
            <i class="fa-solid fa-user me-1"></i>
            <% if(currentUser) { %>
              <%= currentUser.username %>
            <% } else { %>
              Account
            <% } %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3 mt-2" aria-labelledby="userDropdown">
            <% if(!currentUser) { %>
              <li><a class="dropdown-item py-2 px-3" href="/signup"><i class="fa-solid fa-user-plus me-2"></i>Sign Up</a></li>
              <li><a class="dropdown-item py-2 px-3" href="/login"><i class="fa-solid fa-sign-in-alt me-2"></i>Login</a></li>
            <% } else { %>
              <li><a class="dropdown-item py-2 px-3" href="/logout"><i class="fa-solid fa-sign-out-alt me-2"></i>Logout</a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>
